# rpm-ostreed 自动更新策略片段 —— 由 scripts/linux/set-updates.sh 安装到 /etc/rpm-ostreed.conf
# 设计依据:决策 3.18 与设计 4 节(R8 保守更新策略)——自动更新只允许"只检查 / 只下载",
#   不自动应用、不自动重启:原子版下"应用"= 产生新部署,仍需一次重启才生效;无人值守地应用
#   内核/驱动更新,在 NVIDIA 混合显卡机器上等于把"能否进桌面"交给定时任务。
# 分层安装与 rebase 之前先 pin 当前部署(设计 3.18 / R1),再手动变更。
# 生效方式:安装后 sudo systemctl enable --now rpm-ostreed-automatic.timer。
# 回滚:还原 /etc/rpm-ostreed.conf.dbk.bak(或删掉本文件)后重启 rpm-ostreed。
# 待核实(以官方文档为准):[Daemon] 段名与 AutomaticUpdatePolicy 取值以 rpm-ostreed.conf 官方文档为准。
[Daemon]
AutomaticUpdatePolicy=check
